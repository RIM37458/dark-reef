# 暗黑之礁安全说明

## 数据与权限边界

- 正式桌面版会通过 Steam 登录并读取好友及 Dota 2 Game Coordinator 返回的信息。
- Steam 密码仅用于本次登录，不写入配置文件。刷新令牌由 Windows `safeStorage`（DPAPI）加密后保存在当前 Windows 用户的数据目录；系统无法提供加密时，程序不会降级为明文保存。
- 选人和计时识别只读取用户选定的画面，不注入游戏进程、不读写游戏内存、不模拟键鼠，也不修改游戏文件。
- 视觉训练采集只接受经 Windows 进程核验的 `dota2.exe` 窗口，不接受整个显示器；不录音、不上传。JPEG 帧保存在当前用户应用数据目录的 `visual-training-captures` 中，总量上限 768 MB，超限时删除最旧的已结束场次。
- 桌面版不启动本地 HTTP 服务。网络请求仅用于 Steam/Dota 2 登录、好友状态与比赛信息，以及固定的 Steam 头像地址。

## 已实施的桌面隔离

- Electron 渲染进程启用上下文隔离和沙箱，并禁用 Node.js 集成。
- 页面使用内容安全策略；外部导航和新窗口全部拦截。
- 渲染进程仅能调用预加载脚本公开的有限功能，主进程会再次校验 IPC 调用来自本程序创建且仍存活的窗口。
- 配置、英雄池方案和导入内容均经过长度、类型与取值范围校验。

## 分发与剩余风险

- 当前测试包未配置受信任的 Windows 代码签名证书，因此朋友首次运行时可能看到“未知发布者”或 SmartScreen 提示。请通过可信渠道发送，并同时提供 SHA-256 校验值。
- DPAPI 能防止令牌以明文落盘，但不能抵御已在同一 Windows 用户权限下运行的恶意软件。测试者仍应开启 Steam Guard，不要共享令牌文件。
- 视觉训练帧可能包含游戏内聊天、玩家昵称等画面内容；向他人发送应用数据目录前应删除 `visual-training-captures`，这些素材不会随 EXE 自动分发。
- 登录与 Game Coordinator 功能依赖第三方 Node.js 库及 Valve 的非稳定接口，未来更新可能导致失效。建议朋友测试时优先使用非主要账号，且不要让同一 Steam 账号同时在游戏客户端与本程序登录。
- `steam-user` 依赖链中的 `adm-zip` 已固定为修复符号链接覆盖公告的 0.6.1；发布前必须保持依赖审计无未处理的高危或中危公告。

发现安全问题时，请停止分发受影响版本，并保留日志中不含密码或令牌的错误信息以便复现。
