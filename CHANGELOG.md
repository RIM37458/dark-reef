# 更新记录

## 0.17.0 - 2026-09-11

### 新增

- 游戏覆盖层在敌方五名英雄头像下方预留大招与 BKB 自动记录状态。
- 开启覆盖层后，仅对经过 `dota2.exe` 进程核验的窗口采集无声本地视觉训练帧；素材总量限制为 768 MB，并自动清理最旧的已结束场次。

## 0.16.1 - 2026-09-11

### 安全

- 将 `steam-user` 的传递依赖 `adm-zip` 固定到 0.6.1，修复符号链接导致的任意文件覆盖公告。

## 0.16.0 - 2026-09-11

### 安全

- Steam 刷新令牌改用 Windows 系统加密保护；不再以明文 JSON 持久化。
- 正式桌面版不再启动本机 HTTP 状态服务，减少不必要的本地接口。
- 保留 Electron 渲染沙箱、隔离上下文、严格内容安全策略和固定网络目标。

### 变更

- 对外程序与安装包名称统一为“暗黑之礁”，移除 `Monitor` 和 `Watcher` 字样。
